{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secondary-windows",
  "description": "Capabilities for the Connection and Settings secondary windows (P5): events plus the four mutating window controls used by their custom title bar (start-dragging, minimize, toggle-maximize, close). create-webview-window is deliberately NOT granted, so secondary windows cannot spawn further windows. is-maximized is covered by core:default via core:window:default.",
  "windows": ["connection", "settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close"
  ]
}
