{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://norveseditor.dev/bridge/spec/schema/methods/component.remove.result.schema.json",
  "title": "component.remove response result",
  "description": "Whether the engine detached the component. A refusal is reported as accepted:false, not as a protocol error.",
  "type": "object",
  "required": [
    "accepted"
  ],
  "additionalProperties": false,
  "properties": {
    "accepted": {
      "description": "Whether the engine detached the component.",
      "type": "boolean"
    }
  }
}
