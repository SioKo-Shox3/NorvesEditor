{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://norveseditor.dev/bridge/spec/schema/methods/runtime.play.result.schema.json",
  "title": "runtime.play response result",
  "description": "Acknowledgement that the runtime transition request was accepted. The actual state change is carried by the runtime.stateChanged event.",
  "type": "object",
  "required": ["accepted"],
  "additionalProperties": false,
  "properties": {
    "accepted": {
      "description": "Whether the engine accepted the transition request.",
      "type": "boolean"
    },
    "requestedState": {
      "description": "Optional runtime state the engine is targeting. For play this is playing.",
      "$ref": "https://norveseditor.dev/bridge/spec/schema/common.schema.json#/$defs/runtimeState"
    }
  }
}
