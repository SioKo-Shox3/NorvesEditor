//! `norves-bridge-core` — NorvesEditor Bridge core message, status, and value types.
//!
//! Wire-protocol types and JSON codec live here. This crate must not depend on
//! Tauri, the UI layer, or NorvesLib (see CLAUDE.md architecture boundaries).
